# Guidewire

Desktop Accessibility MCP — a Playwright-like MCP server for non-browser desktop applications powered by OS accessibility APIs.

## Status

Early scaffold. See [docs/guidewire_idea.txt](docs/guidewire_idea.txt) for the full project plan.

## Development

```bash
pip install -e ".[dev]"
pytest
ruff check src/ tests/
```
