"""Integration tests for Guidewire MCP server.

Tests in this package boot the full Guidewire MCP server subprocess and
exercise end-to-end flows.  They require the ``[integration]`` extra and
are skipped by default unless ``GUIDEWARE_RUN_INTEGRATION=1`` is set.
"""
